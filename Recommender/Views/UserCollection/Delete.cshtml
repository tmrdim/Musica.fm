@model Recommender.Model.UserCollection

@{
    ViewBag.Title = "Delete";
}
<div class="container body-content" style="text-align: center">
    <h3>Delete</h3>
    <h4>Are you sure you want to delete this collection?</h4>
    <div>
        <hr />
        
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" style="background-color: #339999; color: white;" /> |
                @Html.ActionLink("Back to List", "Index", new { id = ViewBag.UserId })
            </div>
        }
    </div>
</div>